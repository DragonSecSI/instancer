{{- if .Values.flag.path }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-flag
  labels:
    app.kubernetes.io/managed-by: "instancer"
data:
  {{ .Values.flag.path.key }}: {{ .Values.flag.flag | quote }}
{{- end }}
